import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ProjectGridComponent } from './project-grid.component';

xdescribe('ProjectGridComponent', () => {

  beforeEach(() => {
  
  });

  it('should create', () => {
    expect(1).toBeTruthy();
  });
});

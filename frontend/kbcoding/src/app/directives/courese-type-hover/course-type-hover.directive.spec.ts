import { CourseTypeHoverDirective } from '../courese-type-hover/course-type-hover.directive';

describe('CourseTypeHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseTypeHoverDirective();
    expect(directive).toBeTruthy();
  });
});
